def sum(a,b)
{
  echo "The sum of result ${a} and ${b} is ${a+b}"
}
def sub(a,b)
{
  echo "The substraction of result ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
  echo "The multiplication resulto of ${a} and ${b} is ${a*b}"
}
def div(a,b)
{
  echo "The divided result of ${A} and ${B} is ${a/b}"
}
